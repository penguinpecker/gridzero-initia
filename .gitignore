# deps
node_modules/
**/node_modules/

# next.js
app/.next/
app/out/

# move build artifacts + toolchain
move/build/
.tools/

# env / secrets
.env
.env.local
.env.*.local
**/.env
!**/.env.example

# cloud / infra secrets (Hetzner API token, SSH keys)
.hcloud-token
*.token
.secrets/
id_ed25519*
*.pem

# local working references (the original Base source) — not part of this fork
reference/

# vercel link dir
.vercel/
**/.vercel/

# os
.DS_Store
**/.DS_Store
rollup/seed/

# local bug/state log — never commit
records.txt
